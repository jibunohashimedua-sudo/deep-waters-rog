node_modules
.next
.env.local
.env
.DS_Store
*.log
.vercel
.env*

# Downloaded study datasets — large, and not ours to redistribute
scripts/.data/

# TypeScript's incremental build cache — regenerated by every build,
# and never the same twice. Kept on disk so builds stay fast; not in git,
# where it only ever showed up as noise on somebody else's diff.
*.tsbuildinfo
